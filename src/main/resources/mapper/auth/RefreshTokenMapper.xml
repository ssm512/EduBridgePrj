<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.domain.auth.mapper.RefreshTokenMapper">

    <!-- refresh_token 컬럼에는 SHA-256 해시만 저장한다 -->

    <insert id="insertToken" useGeneratedKeys="true" keyProperty="tokenId" keyColumn="token_id">
        INSERT INTO refresh_tokens (user_id, refresh_token, expires_at, revoked_yn)
        VALUES (#{userId}, #{refreshToken}, #{expiresAt}, #{revokedYn})
    </insert>

    <select id="selectByTokenHash" resultType="com.edu.domain.auth.vo.RefreshTokenVo">
        SELECT token_id, user_id, refresh_token, expires_at, revoked_yn, created_at
          FROM refresh_tokens
         WHERE refresh_token = #{tokenHash}
    </select>

    <update id="revokeByTokenHash">
        UPDATE refresh_tokens
           SET revoked_yn = 'Y'
         WHERE refresh_token = #{tokenHash}
    </update>

    <!-- 1인 1토큰 정책: 새 토큰 발급 전 기존 활성 토큰 전부 폐기 -->
    <update id="revokeAllByUserId">
        UPDATE refresh_tokens
           SET revoked_yn = 'Y'
         WHERE user_id = #{userId}
           AND revoked_yn = 'N'
    </update>

    <delete id="deleteExpiredTokens">
        DELETE FROM refresh_tokens
         WHERE expires_at &lt; #{now}
    </delete>

</mapper>
