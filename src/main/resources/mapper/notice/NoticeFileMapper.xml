<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.domain.notice.mapper.NoticeFileMapper">

    <!-- NOT-06 첨부파일 메타데이터 등록 -->
    <insert id="insertFile" useGeneratedKeys="true" keyProperty="fileId" keyColumn="file_id">
        INSERT INTO notice_files (notice_id, original_name, stored_name, file_path, file_size)
        VALUES (#{noticeId}, #{originalName}, #{storedName}, #{filePath}, #{fileSize})
    </insert>

    <!-- NOT-07 첨부파일 단건 조회 (다운로드용) -->
    <select id="selectByFileId" resultType="com.edu.domain.notice.vo.NoticeFileVo">
        SELECT file_id, notice_id, original_name, stored_name,
               file_path, file_size, created_at
          FROM notice_files
         WHERE file_id = #{fileId}
    </select>

    <!-- 공지의 첨부파일 목록 -->
    <select id="selectFilesByNoticeId" resultType="com.edu.domain.notice.vo.NoticeFileVo">
        SELECT file_id, notice_id, original_name, stored_name,
               file_path, file_size, created_at
          FROM notice_files
         WHERE notice_id = #{noticeId}
         ORDER BY file_id
    </select>

    <!-- 첨부파일 삭제 (명세서 외 - 첨부 관리용) -->
    <delete id="deleteFile">
        DELETE FROM notice_files WHERE file_id = #{fileId}
    </delete>

</mapper>
